package Array;

public class ArrayEx {
	public static void main(String[] args) {
		// 1. array Declartion -> type[] arrayname; 
		//initialize
		int arr[] = { 1, 2, 3, 4, 8, 5, 6, 10, 9 };

		// size
		int n = arr.length;

		// trarversing
		for (int i : arr) {
			System.out.print(i + " ");
		}
		
		
		// 2. Create Array-> Creating an array of 5 integer
		//num = new int[5];
		
		
		
		
		//3. Access an  element of an array-> //setting the first element o the array
		 //Number[0] = 10;
		
		//accessing the first element
		// int firstElement = num[0];
		
		
		
		
		
		//4 Change an array element
		// changing the first element to 20
		// number[0]=20;
	
		
		
		
		//5. array length
	     // Getting the length of the array
		// int length = number.length;
		
		
		
		
		
		
	}

}
